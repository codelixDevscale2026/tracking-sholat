{
	"name": "tracking-sholat",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"prepare": "husky",
		"api:dev": "pnpm run --filter=api dev",
		"platform:dev": "pnpm run --filter=platform dev",
		"dev": "pnpm --parallel --filter=api --filter=platform run dev",
		"api:build": "pnpm run --filter=api build",
		"platform:build": "pnpm run --filter=platform build",
		"build": "pnpm --parallel --filter=api --filter=platform run build",
		"lint": "pnpm exec biome check",
		"lint:fix": "pnpm exec biome check --write --unsafe"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "2.4.4",
		"husky": "^9.1.7"
	},
	"dependencies": {
		"dotenv-cli": "^11.0.0"
	}
}
